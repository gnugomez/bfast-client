import { WeekDayToStringPipe } from './week-day-to-string.pipe';

describe('WeekDayToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekDayToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
